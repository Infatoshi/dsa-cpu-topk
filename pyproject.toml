[project]
name = "dsa-cpu-topk"
version = "0.1.0"
description = "DeepSeek Sparse Attention with AVX-512 top-K offload experiment"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pytest>=9.0.3",
    "torch>=2.11.0",
    "triton>=3.6.0",
]
